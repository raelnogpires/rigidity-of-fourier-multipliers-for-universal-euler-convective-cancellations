# Paper source

This directory is the arXiv-ready source package. `manuscript.tex` is the main
file; `references.bib` and `manuscript.bbl` provide the bibliography. The
`supplement/` directory contains the frozen certificate index and reproduction
notes. Generated files, including the compiled PDF, are intentionally excluded.
